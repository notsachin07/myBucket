# myBucket
S3 bucket + AWS lambda + preconfig free azure DB
# Objective
An application using S3 bucket, AWS Lambda and Azure DB cloud drive working with low cost pre-signed url generation strategies. 

Application itself is a attestion and skipping the authentication part.